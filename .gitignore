# Terraform — state와 변수 파일에는 비밀·인프라 정보가 포함될 수 있어 커밋 금지
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json

# 자격증명 — 실습 중 생성되는 키·인증 정보는 절대 커밋 금지
*.pem
*.ppk
*.key
credentials
.aws/
.env
.env.*
!.env.example

# 편집기·OS·도구 로컬 설정
.DS_Store
._*
.idea/
.vscode/
.claude/settings.local.json
